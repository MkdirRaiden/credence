enum UserRole {
  USER
  ADMIN
}

enum OtpChannel {
  SMS
  EMAIL
}

enum RedemptionStatus {
  PENDING
  APPROVED
  REJECTED
}

enum CreditEvent {
  ISSUE
  REVOKE
  REFUND
  EXPIRE
}

enum IdempotencyScope {
  API
  JOB
}

enum IdempotencyStatus {
  STARTED
  SUCCEEDED
  FAILED
}
